<Window x:Class="YP.Pages.WindowAddToList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YP.Pages"
        mc:Ignorable="d"
        Title="WindowAddToList" Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="AntiqueWhite">
    <Grid Margin="15">
        <StackPanel>
            <TextBlock Text="Добавление в список" FontSize="24" HorizontalAlignment="Center" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Title, StringFormat='Книга: {0}'}" FontSize="14" Margin="0,0,0,10"/>
            <TextBlock Text="Выберите статус:" Margin="0,0,0,5" FontSize="14"/>
            <ListBox x:Name="ListBoxStatuses">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <RadioButton GroupName="StatusGroup" Content="{Binding Name}" FontSize="12" Checked="RadioButton_Checked"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button x:Name="BtnAdd" Content="Добавить в список" FontSize="14" Width="180" Height="30" Margin="0,20,0,0" Background="#8895af" Foreground="White" FontWeight="Bold" Click="BtnAdd_Click"/>
        </StackPanel>
    </Grid>
</Window>
